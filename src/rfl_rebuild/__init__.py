"""RFL-Rebuild — new namespace. Nothing here is implemented yet.

The legacy trees are frozen and must stay importable so old results remain
reproducible:

    src/rflnext/   legacy v0.3   (tag legacy-v0.3)
    src/rflv04/    legacy v0.4   (tag legacy-v0.4.1)

This package is the rebuild described in docs/rebuild/. It starts empty on
purpose: the specification is frozen first, implementation second.
"""

__all__: list[str] = []
