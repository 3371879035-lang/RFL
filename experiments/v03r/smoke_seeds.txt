# F0 smoke seed set: five seeds, disjoint from the development set.
# Consumed by `scripts/run_smoke.py --seeds-file experiments/v03r/smoke_seeds.txt`
# so the instrument draws the set F0 enumerates rather than a re-typed range.
0
1
2
3
4
