F0 construction: the balanced ordering and the master acquisition

Brings §3's two objects onto this line as gated code. Construction only: no seed is drawn, no
smoke is run, and F0 remains NOT VALID.

b2/evalorder.py --- §3's balanced master ordering, as the object §3 froze:
* the 96 (kappa, phi, error_flag, z_base) cells visited round-robin with the cause
  rank advancing by two strides coprime with 60 (7q + 37r mod 60 for n = 96q + r);
* a bijection on all 5760 units, with prefix(N) nesting, prefix_digest and coverage so the
  coverage claim is recomputed rather than asserted --- the digest at 1024 is pinned to the
  value pre-data verification recorded (f9ac07213237dbdb), and the canonical A88 order is
  measured at the same prefix to show why it was replaced (kappa 1/2 and phase 3/6 only).

b2/acquisition.py --- BaselineAcquisitionPlan -> acquire_master_baseline -> the matrix plus
sufficient A89 pre-update eligibility material:
* one level per (seed, episode, scene), from the same rollout that supplies the eligibility
  material, with the mean curve a view of the matrix;
* the run's own credited domains, H_pre, and the incidence of the bank's own episodes, per
  (A, sigma, e), with slice_indices / sufficient / error deriving C_r^{A,sigma,e}(c) and
  §3's (|C|, sum V, sum V^2) mechanically;
* the artifact form is measured, not preferred: c ranges over A89 §77.4's production credited
  domains (13824 / 229 / 4), so the finished statistics are 1.1e8 triples on the frozen
  envelope while the incidence that generates them is 1.7e7 pairs --- §3 permits both and the
  "minimum" is the larger object here (scripts/f0_acquisition_size_probe.py);
* the run key is the seed: each run's protocol is the envelope carrying it, so the training
  machinery is never handed a protocol whose seed is not the key that generated its episodes;
* the mean reproduces A91's _mean_return accumulation order, because CPython's sum() is
  Neumaier-compensated and differs by an ulp on these values.

The sufficiency claim is verified rather than asserted: the material's C_r^{A,sigma,e}(c) is
compared against the frozen instrument's own eligibility for the same learner, for every
channel and all six refinements, and the shared value of an uncontacted site is checked
against it too.

The channel registration follows: both modules join environment.PRODUCTION_MODULES and
view.IMPORT_ALLOWLIST (with hashlib/itertools), so the AST, import and dependency layer covers
them, and the regenerated b2_view_gate_selfcheck.json records the widened allowlist in its
mutation tails.

Gates: full suite green, scripts/spec_audit.py exit 0, b2_view_gate_selfcheck 45/45,
a91_training_gate_selfcheck 6/6.
